Youtube ad skipper

after pulling run 

`pip install -r requirements.txt`

When you run the script, it automatically opens a new browser window or tab.

The browser will use the current session, meaning if you're already logged into YouTube, it will keep you logged in.

Make sure that:

You are logged into your YouTube account in the default web browser before running the script.

you might need to close all previous Chrome windows first, if you use another browser you mmight have to change it in the code
